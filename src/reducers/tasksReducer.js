const initialState = {
  // ici le state initial
};

function tasksReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default tasksReducer;
